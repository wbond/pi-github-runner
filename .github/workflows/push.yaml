name: ci

on:
  push:
    branches:
      - "**"
      - '!main'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      -
        name: Build and push
        run: |
            echo "temp step"
